# Hook 101

## Références

- [Documentation officielle de Claude Code Hooks](https://code.claude.com/docs/fr/hooks)
- [Automatiser les workflows avec les hooks](https://code.claude.com/docs/fr/hooks-guide)
- [Référence des hooks](https://code.claude.com/docs/fr/hooks#hook-lifecycle)
## Taxonomie des hooks (scope POC v1)

Le catalogue se concentre sur **Claude Code**.

### Catégories principales

| Catégorie | Exemples de hooks | Cas d'usage |
|---|---|---|
| **Sécurité** | `PreToolUse: Bash` — détection de secrets | Bloquer les commandes destructives, valider les sorties |
| **Contexte** | `UserPromptSubmit` — injection de contexte repo | Ajouter les conventions du projet, le stack tech |
| **Validation** | `PostToolUse: Write` — lint auto après écriture | Déclencher des vérifications qualité |
| **Notification** | `SessionStart`, `SessionStop` — alertes | Logger les sessions, notifier sur Slack |
| **Workflow** | `PreToolUse: WebFetch` — proxy de requêtes | Contrôler les accès réseau des agents |
| **Documentation** | `Stop` — génération de résumé | Créer un changelog automatique en fin de session |

### Structure d'un hook dans le registre

Champs obligatoires dans `registry/registry.json` :

```json
{
  "slug": "pre-bash-secret-detection",
  "name": "Secret Detection Before Bash",
  "benefit": "Never leak credentials in a shell command",
  "category": "security",
  "provider": ["claude-code"],
  "hook_type": "PreToolUse",
  "trigger": "Bash",
  "description": "Intercepts shell commands and scans the prompt for API keys, tokens or passwords before execution.",
  "use_cases": ["Secure CI/CD", "Credential protection", "DevSecOps audit"],
  "implementation": {
    "type": "settings_json",
    "script_path": "$CLAUDE_PROJECT_DIR/.claude/hooks/pre-bash-secret-detection.mjs",
    "config": {
      "hooks": {
        "PreToolUse": [{ "matcher": "Bash", "hooks": [{ "type": "command", "command": "node $CLAUDE_PROJECT_DIR/.claude/hooks/pre-bash-secret-detection.mjs" }] }]
      }
    },
    "code_snippet": "/* auto-généré par sync-hooks.mjs — ne pas éditer ici */"
  },
  "stack": ["typescript"]
}
```

**Champ `benefit`** : une ligne courte (≤ 60 car.), orientée *résultat* ("pourquoi je l'installe"), pas fonctionnalité. C'est le héros de la carte de survol et de la modale. **Obligatoire pour tous les hooks.**

**Champ `code_snippet`** : **ne jamais éditer à la main**. Il est dérivé automatiquement du `.mjs` sur disque par `node .claude/sync-hooks.mjs`. La source de vérité du code est `.claude/hooks/<slug>.mjs`.

### Compatibilité GitHub Copilot

Les hooks Claude Code utilisent `$CLAUDE_PROJECT_DIR` dans les commandes pour résoudre les chemins depuis n'importe quel répertoire. GitHub Copilot ne supporte pas cette variable d'environnement — les commandes doivent utiliser des **chemins relatifs** à la racine du projet.

Pour générer un `settings.json` compatible GitHub Copilot, le CLI propose une option dédiée :

```
Where to install?
  1  This project              ./.claude — committed with your repo
  2  All my projects           ~/.claude — every project on this machine
  3  This GitHub Copilot project  ./.claude — settings.json adapted, committed with your repo
```

L'option **3** (ou le flag `--copilot`) retire `$CLAUDE_PROJECT_DIR/` de toutes les commandes dans `settings.json`. Exemple de transformation :

```json
// Claude Code (projet standard)
{ "command": "node $CLAUDE_PROJECT_DIR/.claude/hooks/pre-bash-secret-detection.mjs" }

// GitHub Copilot (option 3)
{ "command": "node .claude/hooks/pre-bash-secret-detection.mjs" }
```

Le CLI s'utilise alors avec `--copilot` pour l'installation non-interactive :

```bash
npx hookstack-cli@latest install --hooks=pre-bash-secret-detection,load-git-context --copilot
```

### Hooks universels vs. tech-spécifiques

Un hook **sans `stack`** est universel : il s'affiche quel que soit le projet. Un hook **avec `stack`** (`"typescript"`, `"node"`, `"python"`) n'est affiché que si l'utilisateur a sélectionné cette stack dans le catalogue — c'est le mécanisme opt-out du filtre "Your stack".

Règle : annoter `stack` uniquement quand le `code_snippet` le confirme (filtre d'extension, outil non universel). Ne jamais se fier aux `tags` seuls — les agents d'analyse peuvent taguer `pytest` sur un script qui ne lance que Vitest.

```json
{
  "id": "pre-bash-secret-detection",
  "name": "Détection de secrets avant exécution Bash",
  "category": "security",
  "provider": ["claude-code", "copilot-vscode"],
  "hook_type": "PreToolUse",
  "trigger": "Bash",
  "description": "Intercepte toute commande shell et analyse le prompt pour détecter des clés API, tokens ou mots de passe avant exécution.",
  "use_cases": ["CI/CD sécurisé", "Protection de credentials", "Audit DevSecOps"],
  "implementation": {
    "type": "settings_json",
    "config": {},
    "code_snippet": "..."
  },
  "community_examples": [
    { "repo": "github.com/...", "issue_url": "...", "added_by": "claude-code-analysis" }
  ],
  "tags": ["security", "bash", "secrets", "devsecops"],
  "stack": ["typescript"]
}
```
